# Nixpacks build config for Railway.
# Builds the server only (client goes to Vercel).

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd server && npm ci --omit=dev"]

[start]
cmd = "cd server && sh start.sh"
